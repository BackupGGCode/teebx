# --- T2-COPYRIGHT-NOTE-BEGIN ---
# This copyright note is auto-generated by ./scripts/Create-CopyPatch.
# 
# T2 SDE: package/.../php/php.conf
# Copyright (C) 2004 - 2010 The T2 SDE Project
# Copyright (C) 1998 - 2003 ROCK Linux Project
# 
# More information can be found in the files COPYING and README.
# 
# This program is free software; you can redistribute it and/or modify
# it under the terms of the GNU General Public License as published by
# the Free Software Foundation; version 2 of the License. A copy of the
# GNU General Public License can be found in the file COPYING.
# --- T2-COPYRIGHT-NOTE-END ---

# seems not to handle --libdir...
var_append confopt ' ' "--with-libdir=${libdir##*/}"

if [ "$SDECFG_PKG_PHP_EXTRACONFOPT" ]; then
	var_append extraconfopt " " "$SDECFG_PKG_PHP_EXTRACONFOPT"
fi

var_append extraconfopt " " "--without-aspx"
var_append extraconfopt " " "--without-aspx2"
var_append extraconfopt " " "--without-apache"
var_append extraconfopt " " "--without-mysql"
var_append extraconfopt " " "--without-pear"
var_append extraconfopt " " "--disable-cli"
var_append extraconfopt " " "--disable-ipv6"
var_append extraconfopt " " "--disable-simplexml"
var_append extraconfopt " " "--disable-libxml"
var_append extraconfopt " " "--disable-xmlreader"
var_append extraconfopt " " "--disable-xmlwriter"
var_append extraconfopt " " "--disable-dom"
var_append extraconfopt " " "--without-iconv"
var_append extraconfopt " " "--without-gd"
var_append extraconfopt " " "--disable-phar"
var_append extraconfopt " " "--without-pspell"
#
var_append extraconfopt " " "--with-openssl"
var_append extraconfopt " " "--with-zlib --with-bz2"
var_append extraconfopt " " "--with-sqlite"
var_append extraconfopt " " "--with-gettext"
var_append extraconfopt " " "--enable-discard-path"
var_append extraconfopt " " "--enable-sockets"
var_append extraconfopt " " "--enable-bcmath"
var_append extraconfopt " " "--enable-libxml"
#var_append extraconfopt " " "--with-libxml-dir=usr/bin"
#
pkginstalled libxml2 && var_append extraconfopt " " "--with-libxml-dir=$root/$( pkgprefix libxml2)"

php_premake() {
	echo "#define HAVE_SENDMAIL 1" >> main/php_config.h
}

hook_add postmake 5 "cp $confdir/teebx-php.ini $root/usr/lib/php.ini"

# if we don't inject this file php's Makefile will try to download it
#hook_add premake  5 'cp -v `match_source_file -p install-pear-nozlib` pear/install-pear-nozlib.phar'
hook_add premake 6 php_premake

# HACK ALERT
fix_configure() {
	sed -i "s,lib/lib,${libdir##*/}/lib,g" configure
	for i in PHP_LDAP PHP_LDAP_SASL; do
		sed -i "s,$i/lib,$i/${libdir##*/}," configure
	done 
}
[[ $libdir != *lib ]] && hook_add preconf 5 "fix_configure"

# install into sysroot
atstage cross && var_append makeinstopt ' ' "INSTALL_ROOT=$root"
