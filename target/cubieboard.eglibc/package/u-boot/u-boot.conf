# --- T2-COPYRIGHT-NOTE-BEGIN ---
# This copyright note is auto-generated by ./scripts/Create-CopyPatch.
# 
# T2 SDE: package/.../u-boot-sunxy/u-boot-sunxy.conf
# Copyright (C) 2006 - 2013 The T2 SDE Project
# 
# More information can be found in the files COPYING and README.
# 
# This program is free software; you can redistribute it and/or modify
# it under the terms of the GNU General Public License as published by
# the Free Software Foundation; version 2 of the License. A copy of the
# GNU General Public License can be found in the file COPYING.
# --- T2-COPYRIGHT-NOTE-END ---

# like the linux kernel
makeopt="CROSS_COMPILE=$archprefix"
makeinstopt=

board="none"
case "$arch" in
	avr32)
		case "$SDECFG_AVR32_BOARDDEF" in
			atngw100|atstk100*)
				board=$SDECFG_AVR32_BOARDDEF
				;;
			*)
				board=atstk1002
				;;
		esac
		;;
	arm)
		board=$SDECFG_ARM_BOARDDEF
		;;
	*)
		abort "TODO: Add $arch and board configuration"
		;;
esac
hook_add premake 3 'eval $MAKE ${board}_config'

u_boot_install () {
	local toolsbin=$base/build/$SDECFG_ID/TOOLCHAIN/$toolsdir/bin/mkimage
	# if ever someone builds a native mkimage :-)
	atstage native && toolsbin=$root$sbindir/mkimage
	
	install tools/mkimage $toolsbin
	install u-boot.* $root/boot/
}

u_boot_spl_cp() {
	# because it may be built or not depending on board support, check if...
	if [ -f spl/sunxi-spl.bin ] ; then
		cp spl/sunxi-spl.bin $base/build/$SDECFG_ID/TOOLCHAIN/
	fi
}
hook_add postmake 5 'u_boot_install'
hook_add postmake 5 'u_boot_spl_cp'
